# Crie um programa para mostrar todos os elementos de uma matriz (3x3) um a um

M = [['a', 'b', 'c'], 
     ['d', 'e', 'f'], 
     ['g', 'h', 'i']]

for linha in M:
    for coluna in linha:
        print(coluna)