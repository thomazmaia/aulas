# Métodos de strings
# Métodos -> São como funções associadas a uma biblioteca criada por "alguém" para fins específicos associadas a um tipo de dado específico (como strings, por exemplo)

aux = "Abacaxi"
print(aux.capitalize())
print(aux.title())
print(aux.upper())
print(aux.lower())
print(aux.isupper())
print(aux.islower())